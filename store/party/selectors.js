export const selectDetails = state => {
    return state.Info.details
}

export const selectComments = state => {
    return state.Info.comments
}

export const selectStatusData = state => {
    return state.Info.status
}